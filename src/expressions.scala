// Expressions: a unit of code that returns a value after it has been executed

println("An expression block is indicated by {}")

println("Immutable data is a key concept of functional programming")
println("Immutable data is a key concept of functional programming")
println("New data in stored in new variables/values instead of existing ones")
println("Functional programming is non-destructive with reference to values")

println()
println("Using expression blocks to organize code")
val amount = {val x = 5 *20; x + 10}
println("This is better than:")
val x = 5 * 20; val amount = x + 10
println("The last expression in the block determines the return value")
println("Expression blocks are nestable, with each level having its own scope")

